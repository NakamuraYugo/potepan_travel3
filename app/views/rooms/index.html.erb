<div class="container-fluid room-posts">
  <h4 class="pb-4">登録済みルーム一覧</h4>
  <table>
    <thead>
      <tr align="center">
        <th>ルーム画像</th>
        <th>ルーム名</th>
        <th>ルーム紹介</th>
        <th>料金</th>
        <th>作成日</th>
      </tr>
    </thead>
    <tbody>
      <% @rooms.each do |room| %>
        <tr>
          <td>
            <% if room.room_image.blank? %>
              <%= image_tag 'default_room.jpg', class: 'card-img-top img-fluid' %>
            <% else %>
              <%= image_tag room.room_image.url,class: 'card-img-top img-fluid' %> 
            <% end %>
          </td> <!-- room_imageのままだとエラーが出た。urlをつけたり[0]をつけるとエラーがなくなる -->
          <td><%= link_to room.name, {controller: 'rooms', action: 'show', id: room.id} %></td>
          <td><%= room.introduction %></td>
          <td><%= room.price %></td>
          <td><%= room.created_at.strftime('%Y年%m月%d日 %H:%M') %></td>
        </tr>
      <% end %>    
    </tbody>
  </table>
</div>
